@{
    WebSecurity.Logout();
    
    var retUrl = Request.QueryString["ReturnUrl"];
    
    if( retUrl.IsEmpty() )
    {
        Response.Redirect( "~/" );
    }else
    {
        Context.RedirectLocal( retUrl );
    }
}